import { Server } from '@overnightjs/core';
// import TestController from './controllers/TestController';

export function addControllers(server: Server) {
  // server.addControllers(new TestController());
}
